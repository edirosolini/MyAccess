@model MyAccess.Domains.Entities.UserEntity

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    ViewData["Title"] = "Users";
}

@using (@Html.BeginForm("Remove", "Users", FormMethod.Post, new { @class = "user" }))
{
    <div class="form-group row">
        <div class="col-sm-6 mb-3 mb-sm-0">
            @Html.TextBoxFor(model => model.LastName, htmlAttributes: new { @type = "text", @class = "form-control form-control-user", @disabled = true })
            @Html.ValidationMessageFor(model => model.LastName, "", htmlAttributes: new { @class = "text-danger" })
        </div>
        <div class="col-sm-6">
            @Html.TextBoxFor(model => model.FirstName, htmlAttributes: new { @type = "text", @class = "form-control form-control-user", @disabled = true })
            @Html.ValidationMessageFor(model => model.FirstName, "", htmlAttributes: new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        @Html.TextBoxFor(model => model.EmailAddress, htmlAttributes: new { @type = "email", @class = "form-control form-control-user", @disabled = true })
        @Html.ValidationMessageFor(model => model.EmailAddress, "", htmlAttributes: new { @class = "text-danger" })
    </div>

    <button type="submit" class="btn btn-primary btn-user btn-block">Remove Account</button>

    <hr>

    <div class="text-center">
        <a href="@Url.Action("Index", "Users")" class="btn btn-danger">
            <i class="fas fa-chevron-left"></i>
        </a>
    </div>
}